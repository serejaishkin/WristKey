[package]
name = "wristkey-platform-win"
version = "0.1.0"
edition = "2021"

[dependencies]
wristkey-core = { path = "../core" }
tokio = { workspace = true }
tracing = { workspace = true }
async-trait = "0.1"
