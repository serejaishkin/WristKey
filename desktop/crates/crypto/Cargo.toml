[package]
name = "wristkey-crypto"
version = "0.1.0"
edition = "2021"

[dependencies]
p256 = { version = "0.13", features = ["ecdsa"] }
ecdsa = { version = "0.16", features = ["der"] }
sha2 = "0.10"
thiserror = "1"
